import React from 'react';
import { View, Text } from 'react-native';

const TodaysEvent = () => {
  return (
    <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
      <Text>Today's Events Screen</Text>
    </View>
  );
};

export default TodaysEvent;
